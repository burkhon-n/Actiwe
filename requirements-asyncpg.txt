# Alternative requirements for hosting environments with psycopg2 issues
# Use this if psycopg2-binary fails to install

# Core web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Database - Alternative PostgreSQL drivers
SQLAlchemy==2.0.23
# Use asyncpg instead of psycopg2 for better compatibility
asyncpg==0.29.0
# OR use pg8000 as pure Python alternative
# pg8000==1.30.3
alembic==1.13.1

# Configuration
python-dotenv==1.0.0

# Telegram
pyTelegramBotAPI==4.14.0

# Template engine
Jinja2==3.1.2

# Form handling
python-multipart==0.0.6

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# HTTP requests
requests==2.31.0
httpx==0.25.2

# Image processing
Pillow==10.1.0

# WSGI adapter for FastAPI
asgiref==3.7.2

# Additional dependencies for production
starlette==0.27.0